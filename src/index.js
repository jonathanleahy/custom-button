import React, {Component} from "react";

import '../style/button.scss';

export default class CusstomButton extends Component {
    render() {
        return(
            <button>Hello</button>
        )
    }
}